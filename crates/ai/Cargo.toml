[package]
name = "neverwrite-ai"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
neverwrite-types = { path = "../types" }
neverwrite-vault = { path = "../vault" }
neverwrite-index = { path = "../index" }
neverwrite-diff = { path = "../diff" }

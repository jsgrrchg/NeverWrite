[package]
name = "vault-ai-ai"
version = "0.1.0"
edition = "2021"

[dependencies]
vault-ai-types = { path = "../types" }
vault-ai-vault = { path = "../vault" }
vault-ai-index = { path = "../index" }
vault-ai-diff = { path = "../diff" }
vault-ai-state = { path = "../state" }
